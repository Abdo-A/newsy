import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    width: "100%"
  },
  scrollView: {
    alignSelf: "center"
  }
});

export default styles;
